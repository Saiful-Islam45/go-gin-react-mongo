import { TableCell, styled } from "@mui/material";

export const CustomiseTableCell = styled(TableCell)((_props) => ({
    fontWeight: "bold",
    textAlign: "center",
  }));
  export const CustomiseTableCellBody = styled(TableCell)((_props) => ({
    textAlign: "center",
  }));
 