import { styled, Typography } from "@mui/material";

export const HeaderTypoGraphy = styled(Typography)((_props) => ({
    fontWeight: 700,
    textAlign: "center",
    fontSize: "20px",
    padding: "10px",
    backgroundColor: "#D0D0D0",
    borderRadius: "5px 5px 0 0",
    marginTop: "20px",
  }));